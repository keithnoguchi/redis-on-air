---
all:
  vars:
    ansible_host: localhost
    ansible_connection: local
    master_ip: localhost
    quorum: 1
  children:
    cluster:
      children:
        head:
          hosts: localhost
        work:
          hosts: localhost
    head:
      hosts: localhost
    work:
      hosts: localhost
      vars:
        master: true
